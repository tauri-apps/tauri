---
tauri: patch:enhance
"@tauri-apps/api": patch:enhance
---

# Add support for AppLibrary path

Expose the app Library directory on iOS as `BaseDirectory.AppLibrary` / `appLibraryDir()`, and add a Path view to the API example app to exercise path APIs.
