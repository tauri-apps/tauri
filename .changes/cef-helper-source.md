---
"tauri-cli": "patch:bug"
"tauri-bundler": "patch:bug"
---

Build macOS CEF helpers from the application's resolved CEF crate sources. Preserve Git and local patches. Do not use the framework version as a Rust crate version.
