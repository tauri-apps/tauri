---
"tauri": "patch:feat"
"tauri-runtime": "patch:feat"
"tauri-runtime-cef": "patch:feat"
"tauri-runtime-wry": "patch:feat"
"tauri-bundler": "patch:bug"
---

Report renderer termination reasons on every CEF desktop platform. Preserve the callback when the application uses the dynamic runtime. Use the Sequoia backend for RPM signing to remove the vulnerable draft ML-DSA dependency. Keep mirror URL tests independent of shared environment changes.
