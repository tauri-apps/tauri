# crag:auto-start
# Generated from governance.md by crag
# Regenerate: crag compile --target pre-commit
repos:
  - repo: local
    hooks:
      - id: gate-1
        name: "lint: npm run format:check"
        entry: bash -c 'npm run format:check'
        language: system
        pass_filenames: false
        always_run: true
      - id: gate-2
        name: "lint: cargo clippy -- -D warnings"
        entry: bash -c 'cargo clippy -- -D warnings'
        language: system
        pass_filenames: false
        always_run: true
      - id: gate-3
        name: "lint: cargo fmt --check"
        entry: bash -c 'cargo fmt --check'
        language: system
        pass_filenames: false
        always_run: true
      - id: gate-4
        name: "test: npm run test"
        entry: bash -c 'npm run test'
        language: system
        pass_filenames: false
        always_run: true
      - id: gate-5
        name: "test: cargo test"
        entry: bash -c 'cargo test'
        language: system
        pass_filenames: false
        always_run: true
      - id: gate-6
        name: "build: npm run build"
        entry: bash -c 'npm run build'
        language: system
        pass_filenames: false
        always_run: true
      - id: gate-7
        name: "ci (inferred from workflow): pnpm build"
        entry: bash -c 'pnpm build'
        language: system
        pass_filenames: false
        always_run: true
      - id: gate-8
        name: "ci (inferred from workflow): cargo build --manifest-path ..."
        entry: bash -c 'cargo build --manifest-path ./crates/tauri-schema-generator/Cargo.toml'
        language: system
        pass_filenames: false
        always_run: true
      - id: gate-9
        name: "ci (inferred from workflow): cargo build --manifest-path ..."
        entry: bash -c 'cargo build --manifest-path ./crates/tauri-cli/Cargo.toml'
        language: system
        pass_filenames: false
        always_run: true
      - id: gate-10
        name: "ci (inferred from workflow): cargo test --test '*' -- --i..."
        entry: bash -c 'cargo test --test '\''*'\'' -- --ignored'
        language: system
        pass_filenames: false
        always_run: true
      - id: gate-11
        name: "ci (inferred from workflow): pnpm test"
        entry: bash -c 'pnpm test'
        language: system
        pass_filenames: false
        always_run: true
      - id: gate-12
        name: "ci (inferred from workflow): cargo fmt --all -- --check"
        entry: bash -c 'cargo fmt --all -- --check'
        language: system
        pass_filenames: false
        always_run: true
      - id: gate-13
        name: "ci (inferred from workflow): pnpm eslint:check"
        entry: bash -c 'pnpm eslint:check'
        language: system
        pass_filenames: false
        always_run: true
      - id: gate-14
        name: "ci (inferred from workflow): cargo clippy --all-targets -..."
        entry: bash -c 'cargo clippy --all-targets --all-features -- -D warnings'
        language: system
        pass_filenames: false
        always_run: true
      - id: gate-15
        name: "contributor docs (advisory — confirm before enforcing): c..."
        entry: bash -c 'cargo clippy  # from .github/PULL_REQUEST_TEMPLATE.md'
        language: system
        pass_filenames: false
        always_run: true

# crag:auto-end
